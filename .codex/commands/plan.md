# plan

Use `.codex/skills/content-os-planner/SKILL.md`.

Return a `PlanOutput`.

Use an existing `ResearchOutput` or source packet as input.

Do not write, review, repair, or design unless the user explicitly asks for the next step.
