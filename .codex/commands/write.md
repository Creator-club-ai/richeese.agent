# write

Use `.codex/skills/content-os-writer/SKILL.md`.

Return a `CopyOutput`.

Use an approved `PlanOutput` as input.

Do not start new research, re-plan, review, repair, or design unless the user explicitly asks for that.
