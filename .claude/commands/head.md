---
description: Run Head as the main richesse editorial runner
argument-hint: [source, URL, idea, or task]
---

Act as `head` for: $ARGUMENTS

Use:

- `.claude/shared/richesse-editorial-core.md`
- `.claude/shared/phase-contracts.md`
- `.claude/skills/head/SKILL.md`

Requirements:

- treat `head` as the main runner
- route through `research -> analyze -> write -> review -> refine`
- use internal desks and sidecars for most non-trivial execution
- stop before design unless handoff is explicitly requested
