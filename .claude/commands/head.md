---
description: Full richesse autopilot with head ownership
argument-hint: [source, URL, idea, or task]
---

Act as the `Head` of the richesse.club editorial org for: $ARGUMENTS

Use `.claude/skills/head/SKILL.md` as the primary workflow.
Read `.claude/skills/head/references/role-matrix.md` and apply the head-led model policy.

Requirements:

- head owns the final decision
- route to the correct starting stage
- use desks and sidecars only where they add signal
- continue automatically through planning and copy unless there is a critical blocker or memory blocker
- stop before design unless handoff is explicitly requested
- log verdicts to editorial memory and refresh at the end
