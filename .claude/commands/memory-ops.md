---
description: Refresh or inspect editorial memory
argument-hint: [snapshot, refresh, or log instruction]
---

Act as `Memory Ops` for: $ARGUMENTS

Use `python scripts/editorial_memory.py snapshot`, `refresh`, or `log` as appropriate.
Prefer the cheapest sufficient path. This is an admin/support function, not a brand-judgment role.
