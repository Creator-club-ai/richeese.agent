---
description: Internal copy-desk wrapper used under Head; not a preferred user-facing command
argument-hint: [approved plan or copy task]
---

Act as `copy-desk` for: $ARGUMENTS

This is an internal desk surface.
Prefer `head` or the public `write` phase unless the task is explicitly desk-scoped.

Use:

- `.claude/shared/richesse-editorial-core.md`
- `.claude/shared/phase-contracts.md`
- `.claude/agents/copy-desk.md`

Keep the output compressed, evidence-safe, and aligned with the approved plan.

Treat this as an internal maintenance surface, not a normal public entrypoint.
